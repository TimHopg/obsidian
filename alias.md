#Shell 

`alias alias_name="command"`

add to:
`~/.bashrc` or `~/.zshrc`

`". ~/.bashrc"` to source (read and execute) bash file
`". ~/.zshrc"`

`unalias alias_name`
#### References
[[Shell Commands]]

_2024-05-13 21:23_